<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 Not Found</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <!-- Optional: Bootstrap Icons -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      body {
        background-color: #f8f9fa; /* A dark background color */
        color: #212529; /* Light text color */
      }
      .error-container {
        border: 1px solid #495057;
        background-color: #dadee2;
      }
      .sadness-image {
        max-width: 110px;
      }
    </style>
  </head>
  <body
    class="d-flex flex-column justify-content-center align-items-center min-vh-100 p-4"
  >
  <div class="d-flex flex-column flex-md-row justify-content-center align-items-end">
<!-- Image of Sadness -->
<div class="me-md-3 mb-4 mb-md-0">
<img src="/images/sadness.png" alt="Sadness from Inside Out" class="img-fluid rounded-3 sadness-image">
</div>
    <div
      class="text-center p-5 rounded-3 shadow-lg error-container mx-auto"
      style="max-width: 500px"
    >
      <h1 class="display-1 fw-bold text-danger mb-3">404</h1>
      <h2 class="h1 fw-bold mb-4">Page Not Found</h2>

      <!-- Check for passed messages from the server -->
      <% if (typeof message !== 'undefined') { %>
      <p class="lead text-secondary mb-4"><%= message %></p>
      <% } else { %>
      <p class="lead text-secondary mb-4">
        Oops! The page you were looking for doesn't exist.
      </p>
      <% } %> <% if (typeof url !== 'undefined') { %>
      <p class="small text-secondary mb-5">
        It looks like the path <code><%= url %></code> is not valid.
      </p>
      <% } %>

      <a href="/" class="btn btn-primary btn-lg rounded-pill px-4">
        Back to Home
      </a>
    </div>
  </body>
</html>
